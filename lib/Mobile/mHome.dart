import 'package:flutter/material.dart';

class MobileHome extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'TourX Web App is Comming soon...',
          style: TextStyle(color: Colors.green, fontSize: 16),
        ),
      ),
    );
  }
}
